{
    "files.exclude": {
        "/.git": true,
        "/.DS_Store": true,
        "/.vs": true,
        "/.vscode": true,
        "/*.js": {"when": "$(basename).ts"},
        "/*.map": {"when": "$(basename).map"},
        "/dist": true
    }
}